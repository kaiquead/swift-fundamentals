name: iOS CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build-and-test:
    runs-on: macos-14

    steps:
      - name: Project Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Select Xcode 15.2
        run: sudo xcode-select -s /Applications/Xcode_15.2.app

      - name: Build and tests with xcodebuild including SnapshotTests
        run: |
          set -o pipefail
          cd swift-fundamentals
          xcodebuild test \
            -project swift-fundamentals.xcodeproj \
            -scheme swift-fundamentals \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.4' \
            -enableCodeCoverage YES \
            -quiet | xcpretty

      - name: List snapshots (debug)
        run: |
          set -o pipefail
          ls -R /Users/runner/work/**/__Snapshots__ || echo "No snapshots generated"
